# CSV-Expense-Tracker
This is a command-line Expense Tracker built in Python which allows users to add, view, and delete expenses. All data in this application is stored persistently using CSV files so expenses are saved even after closing the program.
